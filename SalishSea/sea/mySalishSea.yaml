# salishsea command processor run description for Salish Sea case
#
# Salish Sea full domain with:
#   Smoothed JdF mouth bathymetry
#   S4-1 uniform initial T and S, depth corrected
#   Open, unstructured western boundary across Strait of Juan de Fuca
#     Tidal forcing
#     Masson, depth corrected, open boundaries
#  All River Monthly run-off forcing
#  Atmospheric forcing 

config_name: SalishSea

paths:
  NEMO-code: ../../../NEMO-code/
  forcing: ../../../NEMO-forcing/
  runs directory: ../../../SalishSea/

grid:
  coordinates: coordinates_seagrid_SalishSea.nc
  bathymetry: bathy_meter_SalishSea2.nc

forcing:
  atmospheric: /ocean/dlatorne/MEOPAR/CGRF/NEMO-atmos/
  initial conditions: /data/sallen/MEOPAR/SalishSea/SpinUp6/SalishSea_00010368_restart.nc
  open boundaries: open_boundaries/
  rivers: rivers/

namelists:
  - BIGnamelist_r6.time              # restart on Sep 21, 1 day
  - BIGnamelist.domain            # new TS initial conditions
  - BIGnamelist.surface           # CGRF, atmos pressure
  - mynamelist.lateral            # FLA, tides files, ssh files, clim TS,DC
  - ../namelist.bottom
  - ../namelist.tracers
  - BIGnamelist_50.dynamics          # 50/100
  - ../namelist.compute.4x4
